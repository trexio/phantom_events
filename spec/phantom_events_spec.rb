# frozen_string_literal: true

RSpec.describe PhantomEvents do
  it "has a version number" do
    expect(PhantomEvents::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
